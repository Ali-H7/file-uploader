<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles.css" />
    <title>File Uploader - My Files</title>
  </head>
  <body>
    <%- include('header') %> <%- include('nav-bar', {selected:2}) %> <% if (files.length === 0) { %> <%-
    include('warning', { text: "You don't have any files yet, Upload to get started." }) %> <% } else { %> <%-
    include('my-files', {files}) %> <% } %>
  </body>
</html>
